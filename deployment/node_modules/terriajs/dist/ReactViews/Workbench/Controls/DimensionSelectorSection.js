"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
import i18next from "i18next";
import { action } from "mobx";
import { observer } from "mobx-react";
import React from "react";
import { withTranslation } from "react-i18next";
import isDefined from "../../../Core/isDefined";
import CommonStrata from "../../../Models/Definition/CommonStrata";
import SelectableDimensions, { MAX_SELECTABLE_DIMENSION_OPTIONS } from "../../../Models/SelectableDimensions";
import Box from "../../../Styled/Box";
import Select from "../../../Styled/Select";
import Spacing from "../../../Styled/Spacing";
import Text from "../../../Styled/Text";
let DimensionSelectorSection = class DimensionSelectorSection extends React.Component {
    setDimensionValue(dimension, event) {
        dimension.setDimensionValue(CommonStrata.user, event.target.value);
    }
    render() {
        var _a;
        const item = this.props.item;
        // Filter out dimensions with only 1 option (unless they have 1 option and allow undefined - which is 2 total options)
        const selectableDimensions = (_a = item.selectableDimensions) === null || _a === void 0 ? void 0 : _a.filter(dim => !dim.disable &&
            isDefined(dim.options) &&
            dim.options.length < MAX_SELECTABLE_DIMENSION_OPTIONS &&
            dim.options.length + (dim.allowUndefined ? 1 : 0) > 1);
        if (!SelectableDimensions.is(item) ||
            !isDefined(selectableDimensions) ||
            selectableDimensions.length === 0) {
            return null;
        }
        return (React.createElement(Box, { displayInlineBlock: true, fullWidth: true },
            React.createElement(Spacing, { bottom: 2 }),
            selectableDimensions.map((dim, i) => {
                var _a;
                return (React.createElement(React.Fragment, { key: dim.id },
                    React.createElement("label", { htmlFor: `${this.props.item.uniqueId}-${dim.id}` },
                        React.createElement(Text, { textLight: true, medium: true, as: "span" },
                            dim.name || dim.id,
                            ":")),
                    React.createElement(Spacing, { bottom: 1 }),
                    React.createElement(Select, { light: true, name: dim.id, id: `${this.props.item.uniqueId}-${dim.id}`, value: typeof dim.selectedId === "undefined"
                            ? "__undefined__"
                            : dim.selectedId, onChange: this.setDimensionValue.bind(this, dim) },
                        (typeof dim.selectedId === "undefined" ||
                            dim.allowUndefined) && (React.createElement("option", { key: "__undefined__", value: "" }, (_a = dim.undefinedLabel) !== null && _a !== void 0 ? _a : i18next.t("workbench.dimensionsSelector.undefinedLabel"))),
                        dim.options.map(option => (React.createElement("option", { key: option.id, value: option.id }, option.name || option.id)))),
                    i < selectableDimensions.length - 1 && React.createElement(Spacing, { bottom: 2 })));
            })));
    }
};
__decorate([
    action
], DimensionSelectorSection.prototype, "setDimensionValue", null);
DimensionSelectorSection = __decorate([
    observer
], DimensionSelectorSection);
export default withTranslation()(DimensionSelectorSection);
//# sourceMappingURL=DimensionSelectorSection.js.map